# AGENTS.md

## Development Rules

- Use `uv` for dependency management
- Run the project with `uv run slackbot`
- Keep subsystems isolated: intake, engine, output
- Write tests in the `tests/` directory
